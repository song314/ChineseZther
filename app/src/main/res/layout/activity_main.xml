<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="zither.ss.sichuan.cn.chinesezither.MainActivity">

    <android.support.design.widget.TabLayout
        android:layout_alignParentTop="true"
        android:id="@+id/sort_list_top_tab"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:tabIndicatorColor="@color/app_orange"
        app:tabSelectedTextColor="@color/app_orange"
        app:tabTextColor="#333333"
        app:tabMode="scrollable"
        app:tabGravity="center"/>

    <LinearLayout
        android:layout_alignParentBottom="true"
        android:id="@+id/sort_list_bottom_bar"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="教学"
            android:layout_height="wrap_content" />
        <Button
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="我"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/sort_list_view_pager"
        android:layout_below="@id/sort_list_top_tab"
        android:layout_above="@id/sort_list_bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</RelativeLayout>
